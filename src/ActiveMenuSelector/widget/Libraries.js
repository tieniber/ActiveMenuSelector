/*
    Libraries file

    Use this to declare any libraries if you are using them in multiple widgets. If this is a single widget, you do not need libraries.
    Switch this on and off by configuring widget.libraries : true/false in package.json
*/
import {
    defineWidget,
} from 'widget-base-helpers';

export default defineWidget('Libraries', false, {

}, null);
